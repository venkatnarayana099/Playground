#include<iostream>
using namespace std;
int main()
{
  //Type your code here.
  int a,b;
  cin>>a>>b;
  if(a<=(b/2))
    cout<<"circle can be inside a square";
  else
    cout<<"circle cannot be inside a square";
  
}